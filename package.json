{
  "name": "react-router-lint-routes",
  "version": "1.1.1",
  "description": "Lint route paths to enforce type-safe href() usage in React Router 7 projects",
  "keywords": [
    "react-router",
    "lint",
    "href",
    "type-safe"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/christophgockel/react-router-lint-routes"
  },
  "license": "MIT",
  "author": "Christoph Gockel",
  "type": "module",
  "bin": {
    "react-router-lint-routes": "dist/main.js"
  },
  "files": [
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "engines": {
    "node": ">=24.16.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "biome check --error-on-warnings",
    "lint:fix": "biome check --write",
    "lint:ci": "biome ci --error-on-warnings",
    "prepublishOnly": "npm run build",
    "start": "node main.ts",
    "test": "vitest",
    "typecheck": "tsc",
    "verify": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/node": "^24.13.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "peerDependencies": {
    "react-router": ">=7.0.0",
    "typescript": ">=5.8.0"
  }
}
